
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
    service.binding/host: 'path={.spec.bootstrapServer.host}'
    service.binding/user: 'path={.spec.credentials.clientID}'
    service.binding/password: 'path={.spec.credentials.clientSecret}'
    service.binding/secret: 'path={.spec.credentials.clientSecret},objectType=Secret'
  creationTimestamp: null
  name: managedkafkaconnections.oas.redhat.com
spec:
  group: oas.redhat.com
  names:
    kind: ManagedKafkaConnection
    listKind: ManagedKafkaConnectionList
    plural: managedkafkaconnections
    singular: managedkafkaconnection
  scope: Cluster
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: ManagedKafkaConnection schema
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ManagedKafkaConnectionSpec contains credentials and connection
            parameters to Managed Kafka
          properties:
            bootstrapServer:
              description: BootstrapServerSpec contains server host information that
                can be used to connecto the Managed Kafka
              properties:
                host:
                  description: Host full host to Managed Kafka Service including port
                  type: string
              type: object
            credentials:
              description: CredentialsSpec specification containing various formats
                of credentials
              properties:
                clientID:
                  description: ClientID Represents username in client credentials
                  type: string
                clientSecret:
                  description: ClientID Represents password in client credentials
                  type: string
                kind:
                  description: Type of the credential format. For example "ClientCredentials"
                  type: string
              type: object
          required:
          - bootstrapServer
          - credentials
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
